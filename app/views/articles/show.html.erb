<!DOCTYPE html>
<html>
<head>
  <title><%= @article.title %> - Detalhes do Artigo</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Incluir o CSS do Bootstrap -->
  <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css', media: 'all', 'data-turbolinks-track': 'reload' %>

  <!-- Carregar o arquivo de estilo específico -->
  <%= stylesheet_link_tag 'home/home', media: 'all', 'data-turbolinks-track': 'reload' %>

  <!-- Incluir a biblioteca jQuery e o JavaScript do Bootstrap -->
  <%= javascript_include_tag 'https://code.jquery.com/jquery-3.6.0.min.js' %>
  <%= javascript_include_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js' %>

  <!-- Carregar o arquivo JavaScript principal -->
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="container mt-4">
    <h1 class="mb-4"><%= @article.title %></h1>

    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title">Detalhes do Artigo</h5>
        <p class="card-text"><strong>Autor:</strong> <%= @article.author || "Autor desconhecido" %></p>
        <p class="card-text"><strong>Conteúdo:</strong> <%= @article.body || "Conteúdo não disponível" %></p>
        <p class="card-text"><strong>Publicado em:</strong> <%= @article.published_at.present? ? @article.published_at.strftime("%d/%m/%Y") : "Data não disponível" %></p>
        <p class="card-text"><strong>Tags:</strong> <%= @article.tags || "Sem tags" %></p>
      </div>
    </div>

    <div class="mt-3">
      <%= link_to 'Editar', edit_article_path(@article), class: 'btn btn-warning mr-2' %>
      <%= link_to 'Excluir', @article, method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-danger mr-2' %>
      <%= link_to 'Voltar', articles_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</body>
</html>
